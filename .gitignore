.next
node_modules
coverage
dist
.env
.env.local
.env.*.local
playwright-report
test-results
*.log
.playwright-mcp
tsconfig.tsbuildinfo
.vercel
.runtime
.tmp
.open-next
.wrangler

# Environment & Secrets (AIOX)
*.key
*.pem

# Dependencies (AIOX)
node_modules/

# Build & Logs (AIOX)
dist/
build/
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt
.env*.local

# Local tooling binaries (not part of the deployable app)
.tools/

# Reverse-engineering workspace — secrets, decompiled binaries, never publish
/KARTODROMO - SISTEMA/
"KARTODROMO - SISTEMA.zip"
KARTODROMO\ -\ SISTEMA.zip

# Impeccable — tooling local de design (skills, hooks, agentes)
.agents/skills/impeccable/
.claude/skills/impeccable/
.cursor/skills/
.cursor/agents/
.gemini/skills/
.github/agents/
.github/skills/
.github/hooks/
.codex/hooks.json
.impeccable/live/