# Story 1.1: Restore production TB50 control path

## Status

Ready for Review

## Story

As a kart track operator, I need `https://kartodromobetim.vercel.app/admin/telao` to read the live LapTime data and control the TB50 services so the production dashboard reflects and changes the physical display.

## Acceptance Criteria

1. Production `/api/livetime-snapshot` returns the current local scraper snapshot instead of a missing-configuration error.
2. Production `/api/telao-layout`, `/api/tb50-page`, and `/api/tb50-display-mode` persist operator changes.
3. Production `/api/viplex-programs` proxies the local ViPlex service without a Next.js dynamic-import bundle error.
4. The public tunnel is recoverable after process or machine restart.
5. Lint, typecheck, tests, and production build pass.
6. Production APIs and `/admin/telao` are validated after deployment.

## Tasks / Subtasks

- [x] Correct the ViPlex route bundle boundary and add regression coverage.
- [x] Restore a public endpoint for the local scraper and ViPlex bridge.
- [x] Configure the target Vercel project with the live snapshot and remote-control endpoints.
- [x] Harden the Cloudflare/Vercel recovery script for the consolidated project.
- [x] Run all quality gates.
- [x] Validate production APIs and the admin authentication boundary.

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- 2026-06-18: Confirmed local `:4010` and `:3000` services are healthy and return the current qualifying session.
- 2026-06-18: Confirmed production LiveTime was unconfigured and production ViPlex failed with `Cannot find module as expression is too dynamic`.
- 2026-06-18: Deployed `dpl_2GzCBpxgDAJyZ4umezVXLUsq97eH` and validated live snapshot, remote layout/page writes, Blob-backed display mode, ViPlex listing, login boundary, and the rendered TB50 grid.

### Completion Notes List

- Replaced the unresolved expression-based ViPlex import with a statically analyzable lazy import.
- Added a verified cloudflared installer and DNS fallback to the recovery script.
- Configured production to use the on-site Cloudflare bridge.
- Updated `react-router-dom` to 7.18.0; production dependency audit reports zero vulnerabilities.
- `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` passed.

### File List

- `docs/stories/1.1.story.md`
- `.env.example`
- `app/api/viplex-programs/route.ts`
- `lib/viplex-programs.ts`
- `package.json`
- `package-lock.json`
- `scripts/ensure-cloudflare-vercel.ps1`

### Change Log

- 2026-06-18: Story created from the operator request and verified production failures.
- 2026-06-18: Production bridge, ViPlex route, dependency security, recovery automation, deployment, and validation completed.
